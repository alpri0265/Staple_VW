# Апаратна частина — Прес-стапель насос-форсунок VW

## Комплектуючі

| Компонент | Модель | Параметри |
|-----------|--------|-----------|
| МК | STM32F407ZET6 (BLACK_F407ZE) | 168 MHz, 512 KB Flash, 128 KB RAM |
| Шаговий двигун | **86HBP113AL4** | NEMA34, 8.5 Нм, вал Ø8 мм, 1.8°, ~6 A |
| Редуктор | **PX86** планетарний | Передавальне число **1:10** |
| Гвинт | T20×4 | Крок 4 мм/оберт |
| Драйвер | **DM556** | 20–50 V, до 5.6 A, мікрокрок до 1/256 |
| Перетворювач рівнів | **74HCT245** | 3.3 V → 5 V, STM32 → DM556 |
| Датчик зусилля | **LCF-6-V 2T** | 2000 кг, живлення 5–12 V |
| АЦП датчика | HX711 | 24-біт, ×128 підсилення, VCC = **5 V** |
| Дисплей | LCD2004 + PCF8574 | I2C, 20×4 символи, адреса 0x27 |
| Керування | Джойстик 2 положення | Pull-up, active LOW |
| Енкодер меню | KY-040 | CLK на EXTI3 |
| Кутовий датчик | **PandAuto P3022-V1-CW360** | 0–5 V аналоговий, 3 дроти (+5V/GND/OUT) |
| Кнопка ZERO | Тактова кнопка | PD4, скидання кута затяжки |
| Кнопка СТОП | Грибок NC, з фіксацією | EXTI0, пріоритет 0 |
| Концевики | 2× NC механічний | Pull-up, нормально LOW (NC замкнутий) |
| Кнопка BOOST | Тактова кнопка | PC6, активація режиму прискорення |
| Потенціометр швидкості | 10 kΩ лінійний | PA2, ADC2_IN2 |

---

## Зміст
1. [Таблиця пінів](#1-таблиця-пінів)
2. [Шаговий двигун 86HBP113AL4](#2-шаговий-двигун-86hbp113al4)
3. [Драйвер DM556 + 74HCT245](#3-драйвер-dm556--74hct245)
4. [Тензодатчик LCF-6-V + HX711](#4-тензодатчик-lcf-6-v--hx711)
5. [Дисплей LCD2004 I2C](#5-дисплей-lcd2004-i2c)
6. [Енкодер KY-040](#6-енкодер-ky-040)
7. [Джойстик](#7-джойстик)
8. [Кнопка СТОП](#8-кнопка-стоп)
9. [Концевики](#9-концевики)
10. [Кутовий датчик PandAuto P3022-V1-CW360](#10-кутовий-датчик-pandauto-p3022-v1-cw360)
11. [Кнопка BOOST + потенціометр швидкості](#11-кнопка-boost--потенціометр-швидкості)
12. [Живлення](#12-живлення)
13. [Чеклист перед увімкненням](#13-чеклист-перед-увімкненням)

---

## 1. Таблиця пінів

| Функція | Пін МК | Порт | Режим GPIO | Примітка |
|---------|--------|------|------------|----------|
| **STEP** | PE0 | GPIOE | Output PP | До 74HCT245 → DM556 PUL+ |
| **DIR** | PE1 | GPIOE | Output PP | До 74HCT245 → DM556 DIR+ |
| **ENABLE** | PE2 | GPIOE | Output PP | LOW = двигун увімкнений |
| **HX_DAT** (DOUT) | PC0 | GPIOC | Input Pull-up | 5V tolerant (FTf); VCC HX711 = 5V |
| **HX_CLK** (SCK) | PC1 | GPIOC | Output PP | Бітбанг |
| **STOP_BTN** | PA0 | GPIOA | Input Pull-up, EXTI0 ↓ | NO кнопка, пріоритет IRQ = 0 |
| **ENC_CLK** | PB3 | GPIOB | Input Pull-up, EXTI3 ↓ | KY-040, пріоритет IRQ = 1 |
| **ENC_DT** | PB1 | GPIOB | Input Pull-up | Читається в EXTI3 ISR |
| **ENC_SW** | PB2 | GPIOB | Input Pull-up | Антидребезг 20 мс (TIM7) |
| **LIMIT_TOP** | PD0 | GPIOD | Input Pull-up | NC до GND: норм. LOW, спрац. HIGH |
| **LIMIT_BOT** | PD1 | GPIOD | Input Pull-up | NC до GND: норм. LOW, спрац. HIGH |
| **JOY_UP** | PD2 | GPIOD | Input Pull-up | LOW = натиснуто |
| **JOY_DOWN** | PD3 | GPIOD | Input Pull-up | LOW = натиснуто |
| **ZERO_BTN** | PD4 | GPIOD | Input Pull-up | LOW = натиснуто; скидання кута |
| **BOOST_BTN** | PC6 | GPIOC | Input Pull-up | LOW = утримано; режим прискорення |
| **SPEED_POT** | PA2 | GPIOA | Analog | ADC2_IN2; потенціометр 0–3.3 V |
| **ANGLE_OUT** | PA1 | GPIOA | Analog | ADC1_IN1; датчик кута 0–5V → дільник → 0–2.5V |
| **I2C1_SCL** | PB6 | GPIOB | AF4, Open-Drain | 100 kHz |
| **I2C1_SDA** | PB7 | GPIOB | AF4, Open-Drain | 100 kHz |

---

## 2. Шаговий двигун 86HBP113AL4

### Характеристики
| Параметр | Значення |
|----------|----------|
| Формфактор | NEMA 34 (86 × 86 мм) |
| Утримуючий момент | **8.5 Нм** |
| Кут кроку | 1.8° (200 кроків/оберт) |
| Фазний струм | ~6 A |
| Конфігурація | 2-фазний, 4-дротовий |
| Вал | Двосторонній, Ø8 мм |

### Розрахунок зусилля на штоку
```
Момент двигуна:     8.5 Нм
× редуктор 10:1:   85 Нм (ефект. ≈ 70 Нм з урахуванням ККД ~0.82)
Крок гвинта T20×4: 4 мм = 0.004 м
ККД гвинтової пари: ~0.40 (трапецієподібна різьба)

F = 2π × 70 × 0.40 / 0.004 ≈ 44 000 Н ≈ 4 500 кг
```

> Практичне обмеження у прошивці: **FORCE_MAX_KG = 2040 кг (20 кН)** — ліміт датчика LCF-6-V 2T.

### Визначення фаз котушок
| Котушка A | Котушка B |
|-----------|-----------|
| Червоний / Синій | Зелений / Чорний |

---

## 3. Драйвер DM556 + 74HCT245

### Чому 74HCT245
STM32 видає 3.3 V. DM556 очікує VIH ≥ 3.5 V (при 5V DVDD).
74HCT245 (серія HCT) приймає 3.3 V на вхід (VIH = 2.0 V) і видає 5 V — ідеальний перетворювач.

### Схема підключення

```
STM32 (3.3 V)       74HCT245             DM556
─────────────       ────────             ─────
PE0 (STEP)  ──────► A1      B1 ────────► PUL+
PE1 (DIR)   ──────► A2      B2 ────────► DIR+
PE2 (ENABLE)──────► A3      B3 ────────► ENA+

                    VCC ◄── 5 V
                    GND ◄── GND
              DIR (pin1) ◄── GND    (напрямок A→B)
             /OE (pin19) ◄── GND    (завжди увімкнено)

DM556:
  PUL− ── GND
  DIR− ── GND
  ENA− ── GND
```

### Налаштування DIP-перемикачів DM556

#### Струм фази (SW1–SW3) — для 86HBP113AL4 (~6 A)
| SW1 | SW2 | SW3 | Пік, A |
|-----|-----|-----|--------|
| **ON** | **ON** | **ON** | **5.6 A** ← використовується |

**SW4 = OFF** — автоматичне зниження струму утримання до 50% через 0.5 с після зупинки.

#### Мікрокрок (SW5–SW8) — прошивка MICROSTEP = 8 → 1600 кроків/об
| SW5 | SW6 | SW7 | SW8 | Кроків/об |
|-----|-----|-----|-----|-----------|
| **ON** | **OFF** | **ON** | **ON** | **1600 ← використовується** |

### Підключення силової частини
```
24–48 V DC PSU
  (+) ────────────────── VCC  ┐ DM556
  (−) ────────────────── GND  ┘
                       [100µF + 100nF між VCC і GND]
DM556 виходи:
  A+ ── Котушка A (червоний)
  A- ── Котушка A (синій)
  B+ ── Котушка B (зелений)
  B- ── Котушка B (чорний)
```

> **Ніколи** не від'єднувати двигун при увімкненій напрузі VMOT.

### Механічні параметри (MICROSTEP = 1/8)
| Параметр | Значення |
|----------|----------|
| Кроків/оберт з мікрокроком | 1600 |
| Кроків/оберт після редуктора | 16 000 |
| **STEPS_PER_MM** | **4000 кроків/мм** |
| TIM7 тік | **500 мкс** |
| SPEED_FAST | 500 steps/s → **7.5 мм/хв** |
| SPEED_SLOW | 200 steps/s → **3.0 мм/хв** |
| SPEED_POT_MAX | 1000 steps/s → **15 мм/хв** (boost) |

---

## 4. Тензодатчик LCF-6-V + HX711

### Характеристики LCF-6-V 2T
| Параметр | Значення |
|----------|----------|
| Номінальне навантаження | **2000 кг (2T)** |
| Живлення моста | **5–12 V** (мінімум 5V!) |
| Вихідна чутливість | ~2 мВ/В |
| Вхідний/вихідний опір | ~350 Ом |

### Схема підключення
```
LCF-6-V (4-дротовий):    HX711:
  Червоний  (EXC+) ─────── E+
  Чорний    (EXC-) ─────── E-
  Зелений   (SIG+) ─────── A+
  Білий     (SIG-) ─────── A-

HX711 → STM32:
  VCC  ─────── 5 V   ← обов'язково 5V для живлення моста датчика
  GND  ─────── GND
  DT   ─────── PC0   ← 5V tolerant пін (FTf), підключати напряму
  SCK  ─────── PC1   ← 3.3V вихід STM32, на практиці достатньо
  RATE ─────── GND   (10 вимірювань/с)
```

> **Чому VCC = 5V:** LCF-6-V потребує мінімум 5V живлення моста.
> PC0 — 5V tolerant (FTf) при цифровому режимі → безпечно підключати напряму.
> PC1 виходить 3.3V — формально нижче VIH при DVDD=5V (3.5V), але на практиці всі стандартні модулі HX711 читають 3.3V сигнал коректно.

### Монтаж
- Кабель тензодатчика екранований; **екран → GND в одній точці** (з боку HX711)
- Кабель тримати якомога далі від силових проводів DM556
- Феритове кільце на кабель поруч із HX711 — зменшує ЕМ перешкоди

---

## 5. Дисплей LCD2004 I2C

### Схема підключення
```
STM32 PB6 (SCL) ──── SCL ┐
STM32 PB7 (SDA) ──── SDA │  PCF8574 + LCD2004
3.3 V або 5 V   ──── VCC │
GND             ──── GND ┘
```

> Pull-up резистори зазвичай вже є на готовому модулі PCF8574.

### I2C адреса
| Чіп | Адреса | config.h |
|-----|--------|----------|
| PCF8574 | **0x27** (за замовчуванням) | `LCD_I2C_ADDR (0x27 << 1)` |
| PCF8574A | 0x3F | змінити на `(0x3F << 1)` |

---

## 6. Енкодер KY-040

### Схема підключення
```
KY-040:    STM32:
  CLK ─── PB3  (EXTI3, Falling edge, Pull-up)
  DT  ─── PB1  (Input Pull-up)
  SW  ─── PB2  (Input Pull-up)
  VCC ─── 3.3 V
  GND ─── GND
```

### Таблиця напрямку
| CLK | DT | Напрямок | Результат |
|-----|----|----------|-----------|
| ↓ | HIGH | ↻ за годинниковою | `enc_delta++` |
| ↓ | LOW  | ↺ проти годинникової | `enc_delta--` |

---

## 7. Джойстик

### Схема підключення
```
UP:   COM → GND,  NO контакт → PD2 (Pull-up)
DOWN: COM → GND,  NO контакт → PD3 (Pull-up)
```

- LOW = натиснуто
- Антидребезг у TIM7 ISR

---

## 8. Кнопка СТОП

### Рекомендований тип
Грибок червоного кольору, **NO (нормально-розімкнений)**, з механічною фіксацією.

### Схема підключення
```
Кнопка СТОП (NO):
  контакт 1 ─── PA0 (EXTI0, Falling, Pull-up, пріоритет 0)
  контакт 2 ─── GND
```

> При відсутності зовнішньої кнопки: вбудована кнопка **KEY/WKUP** плати BLACK_F407ZE також на PA0.

### Логіка (2 натискання)
| Дія | Результат |
|-----|-----------|
| **1-е натискання** | `motor_emergency_stop()` → зупинка; дисплей: `"STOP: press again"` |
| **2-е натискання** на екрані ERROR | `motor_clear_error()` → повернення до роботи |

---

## 9. Концевики

### Тип: NC механічні мікроперемикачі (2 шт.)

### Схема підключення
```
Верхній концевик:       Нижній концевик:
  NC контакт → PD0        NC контакт → PD1
  COM        → GND        COM        → GND
```

### Логіка (NC до GND + Pull-up)
| Стан | Рівень PD0/PD1 | `motor_is_limit()` |
|------|---------------|-------------------|
| Нормальна робота (NC замкнутий) | **LOW** | false — рух дозволено |
| Спрацювання (NC розімкнувся) | **HIGH** | true — зупинка |
| Обрив дроту | **HIGH** (pull-up) | true — зупинка (**fail-safe**) |

> Обрив дроту автоматично блокує рух — безпечна логіка.

### Рівні захисту
| Пріоритет | Де | Дія |
|-----------|-----|-----|
| **Вищий** | TIM7 ISR (500 мкс) | Миттєва зупинка → MOTOR_IDLE; рух у протилежний бік дозволений |
| Нижчий | Main loop | Додатковий контроль |

---

## 10. Кутовий датчик PandAuto P3022-V1-CW360

### Характеристики
| Параметр | Значення |
|----------|----------|
| Тип виходу | **Аналоговий 0–5 V** (абсолютний, 1 провід OUT) |
| Роздільна здатність | ~1° (при АЦП 12-біт) |
| Живлення | **5 VDC** |
| Кількість проводів | 3 (+5V, GND, OUT) |
| Підключення МК | PA1 через дільник 10kΩ/10kΩ → ADC1_IN1 |

### Схема підключення

```
P3022-V1-CW360:        Дільник напруги:        STM32:
  +5V ─────────────────────────────────────── 5V
  GND ─────────────────────────────────────── GND
  OUT ─── R1 (10kΩ) ─┬─ R2 (10kΩ) ─── GND
                      └──────────────────────── PA1 (ADC1_IN1, аналоговий)

Кнопка ZERO:
  контакт 1 ── PD4 (Input Pull-up)
  контакт 2 ── GND
```

> **Чому дільник:** PA1 витримує максимум 3.3V (аналоговий вхід, не 5V tolerant).
> R1=R2=10kΩ → Vpin = Vsensor × 0.5 → при Vsensor=5V: Vpin=2.5V ✓

### Реалізація в прошивці
- ADC1 канал 1 (PA1), опитування polling, 12-біт
- Перетворення: `кут = (ADC_raw/4095 × 3.3V × 2) / 5V × 360°`
- `ANGLE_SENSOR_V_MAX = 5.0f` та `ANGLE_DIVIDER_RATIO = 0.5f` — у config.h
- Якщо реальний діапазон датчика відрізняється від 0–5V — відкорегувати `ANGLE_SENSOR_V_MAX`

### Кнопка ZERO
- Натиснути перед початком затяжки — зберігає поточний кут як нульову точку
- `torque_angle_get_deg()` повертає кут відносно збереженої точки
- Антидребезг: перевірка переднього фронту + `DEBOUNCE_MS` у main loop

### Відображення (рядок 3, SCREEN_MAIN)
```
Angle: 145.0 /  90.0
```
- Ліворуч: поточний кут від нуля
- Праворуч: цільовий кут (задається в SCREEN_SETTINGS, крок 5°)
- При досягненні → рядок блимає `[  DONE!  ]`

### Налаштування цільового кута
- Меню → Settings → енкодер KY-040 змінює ціль (крок 5°)
- Діапазон: 0°–360°
- Підтвердження: натискання ENC → збереження у Flash

---

## 11. Кнопка BOOST + потенціометр швидкості

### Призначення
Кнопка BOOST вмикає режим прискорення. Потенціометр задає швидкість у цьому режимі — від мінімальної до максимальної.

### Схема підключення

```
Кнопка BOOST (NO):          Потенціометр 10kΩ (лінійний):
  контакт 1 ── PC6 (Pull-up)   лівий кінець  ── GND
  контакт 2 ── GND              середній (вихід) ── PA2
                                правий кінець ── 3.3 V
```

### Логіка роботи
| Стан | Швидкість двигуна |
|------|-----------------|
| Джойстик, кнопка BOOST не натиснута | `SPEED_FAST` = 7.5 мм/хв |
| Джойстик + BOOST утримано | від потенціометра: **0–15 мм/хв** |
| Поблизу цільового зусилля (≥ 80%) | `SPEED_SLOW` завжди, незалежно від BOOST |

### Константи (config.h)
| Константа | Значення | Реальна швидкість |
|-----------|----------|------------------|
| `SPEED_POT_MIN` | 50 | ~0.75 мм/хв |
| `SPEED_POT_MAX` | 1000 | ~15 мм/хв |
| `SPEED_FAST` | 500 | ~7.5 мм/хв (без boost) |

### Реалізація
- PA2 → ADC2_IN2, polling, 12-біт
- `speedpot_get_speed()` лінійно відображає 0–4095 → `SPEED_POT_MIN`–`SPEED_POT_MAX`

---

## 12. Живлення

### Структура живлення
```
~220 V AC
    │
    ├─ [БЖ 36–48 V DC / ≥5 A] ──────────── VCC DM556 (VMOT)
    │                                        └─ [DC-DC 5 V] ─┐
    └─ [БЖ 5 V / 1 A] ──────────────────────────────────────┘
              │
              ├─ BLACK_F407ZE (USB/5V) → [LDO 3.3V] → STM32 + 3.3V периферія
              ├─ HX711 VCC = 5V
              ├─ PandAuto P3022 VCC = 5V
              └─ LCD2004 VCC
```

### Рекомендовані параметри
| Шина | Напруга | Струм | Споживачі |
|------|---------|-------|-----------|
| VMOT | **36–48 V** | ≥ 5 A | DM556 + 86HBP113AL4 |
| VDD | 5 V | 1 A | STM32, HX711, кутовий датчик, LCD |
| 3.3 V | 3.3 V | 200 мА | STM32 I/O, потенціометр |

### Правила заземлення
- **Зіркове заземлення** — VMOT GND і STM32 GND в одній точці
- Конденсатор **100 µF + 100 nF** поруч із DM556 між VCC і GND
- Феритова намистина на кабелях HX711

---

## 13. Чеклист перед увімкненням

### Монтаж
- [ ] VMOT полярність правильна; конденсатор 100 µF встановлений
- [ ] DM556 SW1=ON SW2=ON SW3=ON (5.6A), SW4=OFF (auto-half)
- [ ] DM556 SW5=ON SW6=OFF SW7=ON SW8=ON (1600 кроків/об, 1/8 мікрокрок)
- [ ] 74HCT245: VCC=5V, GND, DIR=GND, /OE=GND; A1=PE0, A2=PE1, A3=PE2
- [ ] Котушки двигуна: A+/A− і B+/B− підключені та перевірені
- [ ] HX711 VCC = **5V**; DT → PC0, SCK → PC1
- [ ] Тензодатчик: E+/E−/A+/A− підключені (перевірити кольорову маркування)
- [ ] LCD I2C адреса: 0x27 (або 0x3F); Pull-up на SDA/SCL є
- [ ] Концевики: NC контакт → PD0/PD1, COM → GND; при відключеному дроті мультиметром PD0/PD1 = HIGH
- [ ] Кнопка СТОП NO: контакт 1 → PA0, контакт 2 → GND
- [ ] Кутовий датчик: OUT → дільник R1/R2 (10kΩ/10kΩ) → PA1; VCC=5V, GND
- [ ] Кнопка ZERO → PD4, GND
- [ ] Кнопка BOOST → PC6, GND
- [ ] Потенціометр: лівий→GND, середній→PA2, правий→3.3V
- [ ] Зіркове заземлення: VMOT GND і STM32 GND в одній точці

### Перша перевірка (покроково)
1. Подати живлення **тільки на логіку** (5V / 3.3V), **без VMOT**
2. LCD → головний екран `"VW NF PRESS"` з цифрами ✓
3. Крутити енкодер → значення `T:` змінюється
4. Натиснути СТОП → `"STOP: press again"` → натиснути ще раз → головний екран
5. Концевики: замкнути PD0 на 3.3V → двигун не запускається
6. Кутовий датчик: повернути вісь → `Angle:` змінюється; натиснути ZERO → 0.0
7. Boost + потенціометр: утримати PC6, повернути потенціометр → зміни швидкості перевіряти після підключення VMOT
8. Подати VMOT (36–48 V)
9. Джойстик вгору 1 сек → двигун обертається; відпустити → зупинка
10. Перевірити напрямок: якщо навпаки — поміняти місцями котушку A або B
11. Спрацювання концевика вручну → двигун зупиняється
12. Виконати **калібровку** → меню → Calibration

---

*Прошивка: STM32F407ZET6, 168 MHz | Flash: 6.2% (32 KB) | RAM: 1.0% (1376 B) | TIM7: 500 мкс*
