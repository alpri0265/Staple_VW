# Апаратна частина — Прес-стапель насос-форсунок VW

## Зміст
1. [Мікроконтролер](#1-мікроконтролер)
2. [Повна таблиця пінів](#2-повна-таблиця-пінів)
3. [Шаговий двигун та драйвер](#3-шаговий-двигун-та-драйвер)
4. [Тензодатчик та HX711](#4-тензодатчик-та-hx711)
5. [Дисплей LCD2004 (I2C)](#5-дисплей-lcd2004-i2c)
6. [Енкодер KY-040](#6-енкодер-ky-040)
7. [Джойстик](#7-джойстик)
8. [Кнопка СТОП](#8-кнопка-стоп)
9. [Концевики](#9-концевики)
10. [Живлення](#10-живлення)
11. [Перевірка перед увімкненням](#11-перевірка-перед-увімкненням)

---

## 1. Мікроконтролер

**Плата:** BLACK STM32F407ZE (BLACK_F407ZE)  
**МК:** STM32F407ZET6, корпус LQFP-144  
**Тактування:** HSE 8 MHz → PLL → **168 MHz**  
**Flash:** 512 KB | **RAM:** 128 KB  

> Плата має вбудований 3.3 V LDO. Живлення подається через USB або роз'єм 5 V.

---

## 2. Повна таблиця пінів

| Функція | Пін МК | Порт | Режим GPIO | Примітка |
|---------|--------|------|------------|----------|
| **STEP** (крок двигуна) | PE0 | GPIOE | Output PP | Передній фронт = 1 крок |
| **DIR** (напрямок) | PE1 | GPIOE | Output PP | HIGH = вгору |
| **ENABLE** (увімкнення драйвера) | PE2 | GPIOE | Output PP | LOW = активний |
| **HX_DAT** (DOUT HX711) | PC0 | GPIOC | Input Pull-up | LOW = дані готові |
| **HX_CLK** (PD_SCK HX711) | PC1 | GPIOC | Output PP | Бітбанг, до 1 MHz |
| **STOP_BTN** (кнопка СТОП) | PA0 | GPIOA | Input Pull-up, EXTI0 ↓ | Пріоритет IRQ = 0 |
| **ENC_CLK** (CLK енкодера) | PB3 | GPIOB | Input Pull-up, EXTI3 ↓ | Пріоритет IRQ = 1 |
| **ENC_DT** (DT енкодера) | PB1 | GPIOB | Input Pull-up | Читається в EXTI3 ISR |
| **ENC_SW** (кнопка енкодера) | PB2 | GPIOB | Input Pull-up | Антидребезг 20 мс |
| **LIMIT_TOP** (верхній концевик) | PD0 | GPIOD | Input Pull-up | LOW = спрацював (NC) |
| **LIMIT_BOT** (нижній концевик) | PD1 | GPIOD | Input Pull-up | LOW = спрацював (NC) |
| **JOY_UP** (джойстик вгору) | PD2 | GPIOD | Input Pull-up | LOW = натиснуто |
| **JOY_DOWN** (джойстик вниз) | PD3 | GPIOD | Input Pull-up | LOW = натиснуто |
| **I2C1_SCL** (LCD2004) | PB6 | GPIOB | AF4, Open-Drain | 100 kHz |
| **I2C1_SDA** (LCD2004) | PB7 | GPIOB | AF4, Open-Drain | 100 kHz |

---

## 3. Шаговий двигун та драйвер

### Сумісні драйвери
- **A4988** (до 2 A, мікрокрок до 1/16)
- **DRV8825** (до 2.5 A, мікрокрок до 1/32) — рекомендовано

### Налаштування мікрокроку
Прошивка налаштована на **1/8 мікрокроку** (`MICROSTEP = 8`).

| MS1 | MS2 | MS3 | Мікрокрок | A4988 | DRV8825 |
|-----|-----|-----|-----------|-------|---------|
| LOW | LOW | LOW | 1/1 | ✓ | ✓ |
| HIGH | LOW | LOW | 1/2 | ✓ | ✓ |
| LOW | HIGH | LOW | 1/4 | ✓ | ✓ |
| **HIGH** | **HIGH** | **LOW** | **1/8** | **✓** | **✓** |
| HIGH | HIGH | HIGH | 1/16 | ✓ | — |

> MS1, MS2, MS3 підключити відповідно до таблиці вище. Незадіяні піни MS залишити LOW (GND).

### Схема підключення

```
STM32 PE0 (STEP)   ──────────── STEP  ┐
STM32 PE1 (DIR)    ──────────── DIR   │
STM32 PE2 (ENABLE) ──────────── ENABLE│  A4988 / DRV8825
GND (MS1=HIGH)     ── 3.3V ─── MS1   │
GND (MS2=HIGH)     ── 3.3V ─── MS2   │
GND                ──────────── MS3   │
                                GND   ├── GND (силовий)
                                VMOT  ├── 12–24 V (живлення двигуна)
                                VDD   ├── 3.3 V або 5 V (логіка)
                           1A,1B,2A,2B┘── котушки двигуна
```

### Важливо
- **ENABLE = LOW** → драйвер активний, двигун утримується
- **ENABLE = HIGH** → драйвер вимкнений, двигун вільно обертається
- Конденсатор **100 µF / 35 V** між VMOT і GND — обов'язково, поруч із драйвером
- Конденсатор **100 nF** між VDD і GND — керамічний, поруч із драйвером
- **Налаштування струму обмеження** (Vref) виконується підстроювальним резистором на драйвері **до підключення двигуна**
- Ніколи не від'єднувати двигун під напругою VMOT — спалить драйвер

### Механічні параметри (для довідки)
| Параметр | Значення |
|----------|----------|
| Кроків/оберт двигуна | 200 (1.8°) |
| Мікрокрок | 1/8 |
| Передавальне число редуктора | 20:1 |
| Крок гвинта T20×4 | 4 мм/оберт |
| **Кроків на 1 мм** | **8000** |
| Швидкість (грубий рух) | ~500 кроків/с ≈ 3.75 мм/хв |
| Швидкість (підхід) | ~250 кроків/с ≈ 1.9 мм/хв |

---

## 4. Тензодатчик та HX711

### Схема підключення HX711

```
Load Cell (тензодатчик 4-дротовий):
  Червоний  (E+)  ──── E+   ┐
  Чорний    (E-)  ──── E-   │  HX711
  Білий     (A-)  ──── A-   │  модуль
  Зелений   (A+)  ──── A+   ┘

HX711 → STM32:
  VCC  ──── 3.3 V (або 5 V — залежить від модуля)
  GND  ──── GND
  DT   ──── PC0  (STM32, Input Pull-up)
  SCK  ──── PC1  (STM32, Output PP)
  RATE ──── GND  (10 SPS) або VCC (80 SPS)
```

### Важливо
- HX711 живити від **3.3 V** — забезпечує сумісність рівнів з STM32 без резисторів
- Якщо модуль розрахований на 5 V — перевірити наявність конвертера рівнів або замінити LDO на модулі
- RATE = GND (10 відліків/с) — менший шум, рекомендовано для пресу
- RATE = VCC (80 відліків/с) — вища частота, більший шум
- **Довжину дроту тензодатчика** тримати мінімальною, скрутити пари (E+/E- та A+/A-)
- Екранований кабель значно покращує стабільність показань

### Калібровка
Калібровка виконується через меню → **Calibration**:
1. Зняти навантаження → підтвердити (tare)
2. Покласти еталонну гирю → підтвердити
3. Ввести масу гирі енкодером (крок 0.1 кг)
4. Зберігається автоматично у Flash (Sector 7, 0x08060000)

---

## 5. Дисплей LCD2004 (I2C)

### Модуль
LCD 20×4 символи з I2C-адаптером на базі **PCF8574** або **PCF8574A**.

### I2C адреса
| Мікросхема | Адреса (7 біт) | Зміна в config.h |
|------------|---------------|------------------|
| PCF8574 | **0x27** (за замовчуванням) | `LCD_I2C_ADDR (0x27 << 1)` |
| PCF8574A | 0x3F | `LCD_I2C_ADDR (0x3F << 1)` |

> Якщо дисплей не ініціалізується — спробувати адресу `0x3F << 1`.  
> Адресу можна визначити I2C-сканером.

### Схема підключення

```
STM32 PB6 (SCL) ──[4.7kΩ → 3.3V]──── SCL  ┐
STM32 PB7 (SDA) ──[4.7kΩ → 3.3V]──── SDA  │  PCF8574
3.3 V           ──────────────────── VCC  │  I2C адаптер
GND             ──────────────────── GND  ┘  + LCD2004
```

### Важливо
- Pull-up резистори **4.7 kΩ** на SCL і SDA до 3.3 V — **обов'язково** (якщо їх немає на модулі)
- Більшість готових модулів LCD+PCF8574 мають вбудовані pull-up на 4.7 kΩ до VCC
- Якщо VCC модуля = 5 V, а pull-up підтягнуті до 5 V — **небезпечно** для STM32 (I2C пін не 5V-tolerant на F4!)
  - Рішення: живити модуль від 3.3 V, або використати резисторний дільник/конвертер рівнів
- Довжина шини I2C — до 1 м при 100 kHz, менше = краще

### Розкладка екранів
```
Головний екран:           Меню:
┌────────────────────┐    ┌────────────────────┐
│ VW NF PRESS        │    │ === MENU ===        │
│ Force:   12.4 kg   │    │>1.Work mode         │
│ Target:  15.0 kg   │    │ 2.Calibration       │
│ [^v] ENC    STOP   │    │ 3.Settings          │
└────────────────────┘    └────────────────────┘
```

---

## 6. Енкодер KY-040

### Схема підключення

```
KY-040:           STM32:
  CLK  ──────────── PB3  (EXTI3, Falling, Pull-up)
  DT   ──────────── PB1  (Input Pull-up)
  SW   ──────────── PB2  (Input Pull-up)
  VCC  ──────────── 3.3 V
  GND  ──────────── GND
```

### Логіка напрямку (KY-040)
| CLK (Falling edge) | DT | Напрямок | Дія |
|--------------------|----|----------|-----|
| ↓ | HIGH | За годинниковою ↻ | `enc_delta++` (збільшення зусилля) |
| ↓ | LOW | Проти годинникової ↺ | `enc_delta--` (зменшення зусилля) |

### Важливо
- CLK підключений на EXTI3 (PB3) — апаратне переривання, без пропуску імпульсів
- Антидребезг кнопки SW — програмний, 20 мс (TIM7 1 мс тік)
- Мінімальна довжина дротів від енкодера (< 30 см) — зменшує перешкоди
- Не використовувати довгі дроти без екранування на CLK/DT — можливі хибні спрацювання

---

## 7. Джойстик

Дворежимний джойстик (2 мікроперемикачі: вгору / вниз).

### Схема підключення

```
Джойстик:         STM32:
  UP   ──────────── PD2  (Input Pull-up)
  DOWN ──────────── PD3  (Input Pull-up)
  COM  ──────────── GND
```

### Логіка
- **Натиснуто = LOW** (внутрішній Pull-up підтягує до 3.3 V, кнопка замикає на GND)
- Антидребезг — програмний, 20 мс (в TIM7 ISR)
- Одночасне утримання UP і DOWN: обидва сигнали активні, але у прошивці перевіряються незалежно — якщо обидва натиснуті одночасно, двигун зупиняється (умова `else if (!joy_up && !joy_down)` не виконується і нова команда не видається)

---

## 8. Кнопка СТОП

**Аварійна кнопка з фіксацією** (NC нормально-замкнений тип — рекомендовано) або без фіксації.

### Схема підключення

```
СТОП кнопка:      STM32:
  NC контакт ──── PA0  (EXTI0, Falling, Pull-up, пріоритет 0)
  Інший вивід ─── GND
```

### Логіка роботи
1. **Натискання СТОП** → EXTI0 ISR (найвищий пріоритет):
   - Миттєва зупинка двигуна (`motor_emergency_stop()`)
   - Встановлення прапора зупинки
2. **Головний цикл** → показує екран помилки `"STOP: press again"`
3. **Повторне натискання СТОП** → скидання стану, повернення до роботи

### Важливо
- Пріоритет EXTI0 = **0** (найвищий серед усіх переривань проекту)
- Рекомендується **грибовидна кнопка з фіксацією** для промислового застосування
- Після спрацювання двигун заблокований — продовження роботи тільки після підтвердження повторним натисканням СТОП
- При використанні кнопки NC: якщо дріт обривається — кнопка спрацьовує (fail-safe)

---

## 9. Концевики

**Тип:** механічні мікроперемикачі, **NC** (нормально-замкнені), 2 шт.

### Схема підключення

```
Концевик TOP (верхній):    STM32:
  NC вивід ─────────────── PD0  (Input Pull-up)
  COM вивід ──────────────  GND

Концевик BOT (нижній):     STM32:
  NC вивід ─────────────── PD1  (Input Pull-up)
  COM вивід ──────────────  GND
```

### Логіка
- **Нормально:** PD0/PD1 = HIGH (NC контакт замкнений, підтягує до 3.3V через внутрішній pull-up)
- **Спрацювання:** PD0/PD1 = LOW (NC контакт розмикається)

### Рівні захисту
| Пріоритет | Рівень | Дія |
|-----------|--------|-----|
| 1 (вищий) | TIM7 IRQ (1 мс) | Миттєва зупинка, двигун → IDLE |
| 2 | Головний цикл | Додатковий контроль, зупинка |

> Після спрацювання концевика двигун переходить у стан **IDLE** (не ERROR) — рух у протилежний бік дозволений негайно.

### Важливо
- **NC тип обов'язково** — при обриві дроту концевик "спрацьовує", fail-safe
- Встановлювати концевики так, щоб спрацювання відбувалося **до** механічного упору
- Додатковий механічний упор бажаний як резервний захист

---

## 10. Живлення

### Структура живлення

```
220 V AC
    │
    ├─── [Блок живлення 24 V / ≥3 A] ──── VMOT (драйвер двигуна)
    │                                   └── [DC-DC 5 V] ──┐
    │                                                      │
    └─── [Блок живлення 5 V / 1 A]  ── або ──────────────┘
              │
              ├──── USB / 5V pin платформи BLACK_F407ZE
              │         │
              │         └── [LDO 3.3 V на платі] ──── STM32 + периферія 3.3 V
              │
              └──── LCD2004 VCC (якщо 5 V модуль)
```

### Рекомендовані параметри

| Шина | Напруга | Струм (мін.) | Споживачі |
|------|---------|--------------|-----------|
| VMOT | 12–24 V | 2–3 A | Шаговий двигун |
| VDD_logic | 5 V | 500 mA | STM32, LCD, енкодер |
| 3.3 V | 3.3 V | 200 mA | STM32 I/O, HX711 |

### Важливо
- **Силова земля (VMOT GND) і логічна земля (GND МК) мають бути з'єднані в одній точці** — star grounding
- Конденсатор 100 µF + 100 nF поруч із кожним блоком живлення
- Феритові намистини на лініях живлення HX711 — зменшують перешкоди від двигуна

---

## 11. Перевірка перед увімкненням

### Чеклист монтажу

- [ ] Перевірити полярність живлення VMOT (24 V) і GND
- [ ] Перевірити, що ENABLE пін підключений (PE2 → EN драйвера)
- [ ] Підключити конденсатор 100 µF між VMOT і GND на драйвері
- [ ] Встановити мікрокрок 1/8 на драйвері (MS1=HIGH, MS2=HIGH, MS3=LOW)
- [ ] Налаштувати струм обмеження драйвера *до* підключення двигуна
- [ ] Перевірити правильність фазування котушок двигуна (A1-A2, B1-B2)
- [ ] Перевірити pull-up резистори на I2C (PB6, PB7) — 4.7 kΩ до 3.3 V
- [ ] Перевірити адресу LCD (0x27 або 0x3F)
- [ ] Перевірити підключення тензодатчика (E+, E-, A+, A-)
- [ ] Перевірити НЗ-тип концевиків (при відключеному дроті — лінія LOW)
- [ ] Кнопка СТОП: перевірити спрацювання без подачі живлення на двигун

### Перша перевірка після монтажу
1. Подати живлення **тільки на логіку** (без VMOT)
2. Перевірити, що на LCD з'явилося головне меню
3. Перевірити реакцію енкодера (зміна Target на дисплеї)
4. Перевірити кнопку СТОП → дисплей показує `"STOP: press again"`
5. Подати живлення VMOT
6. Джойстик вгору — двигун повинен обертатися
7. Перевірити спрацювання концевиків — двигун зупиняється
8. Виконати калібровку через меню

---

*Версія прошивки: Етапи 1–8 завершено. Flash: 3.9%, RAM: 0.6%*
