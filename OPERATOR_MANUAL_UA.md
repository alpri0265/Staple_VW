# Інструкція Оператора Стапеля

Версія документа під поточну прошивку проекту `Staple_VW`.

## 1. Призначення органів керування

| Орган керування | Призначення |
|---|---|
| `JOY_UP` | Рух стапеля вгору |
| `JOY_DOWN` | Рух стапеля вниз, вихід назад у меню, зупинка `AUTO` |
| `ENCODER` обертання | Зміна сили, вибір пунктів меню, зміна параметрів |
| `ENCODER` кнопка | Вхід у меню, підтвердження вибору, збереження |
| `ENCODER` утримання + обертання | Тонка підстройка положення |
| `ZERO` | Обнулення кута абсолютного кутового енкодера |
| `STOP` | Аварійна зупинка, підтвердження скидання помилки |
| Потенціометр швидкості | Регулювання швидкості ручного руху |

## 2. Головний екран

На головному екрані відображаються:

- верхній рядок: активний пресет або стандартний напис ручного режиму
- сила: поточне та задане значення
- кут: поточний та цільовий кут
- нижній рядок: режим руху і швидкість

Позначення режимів:

| Напис на екрані | Значення |
|---|---|
| `IDLE` | Стапель стоїть |
| `APPR` | Швидкий підхід |
| `SOFT` | М'який підхід |
| `PRESS` | Робочий дотиск |
| `BACK` | Рух вниз / відхід |
| `FINE` | Тонка підстройка |
| `ASEEK` | Пошук сили в `AUTO` |
| `HOLD` | Утримання в `AUTO` |
| `DONE` | Режим завершено |

## 3. Ручне керування

### Рух вгору

1. Переконайтесь, що на головному екрані немає помилки.
2. Встановіть бажану швидкість потенціометром.
3. Натисніть `JOY_UP`.
4. Для зупинки відпустіть `JOY_UP`.

### Рух вниз

1. Встановіть бажану швидкість потенціометром.
2. Натисніть `JOY_DOWN`.
3. Для зупинки відпустіть `JOY_DOWN`.

### Тонка підстройка

1. На головному екрані натисніть і утримуйте кнопку енкодера.
2. Крутіть енкодер:
   - в один бік -> дрібні кроки вгору
   - в інший бік -> дрібні кроки вниз
3. Відпустіть кнопку енкодера для зупинки тонкої підстройки.

### Зміна цільового зусилля

1. На головному екрані не натискайте кнопку енкодера.
2. Просто крутіть енкодер.
3. Цільове зусилля буде змінюватися кроками.

## 4. Вхід у меню

Щоб відкрити меню:

1. Коротко натисніть кнопку енкодера.
2. Відпустіть кнопку енкодера.
3. На екрані з'явиться `MENU`.

Примітка:

- меню відкривається саме по відпусканню кнопки
- якщо перед цим використовувалась тонка підстройка, меню не відкриється

## 5. Таблиця керування меню

| Екран | Обертання енкодера | Кнопка енкодера | `JOY_DOWN` |
|---|---|---|---|
| `MENU` | Перехід по пунктах | Вибір пункту | Вихід на головний екран |
| `PRESET` | Вибір пресету | Підтвердити пресет | Вийти без вибору |
| `CALIBRATION` | Зміна маси еталона на етапі вводу | Підтвердження кроку | Не використовується для виходу |
| `SETTINGS` | Зміна `Angle target` | Зберегти і повернутися в меню | Вийти без збереження |
| `AUTO` | Не використовується | Не використовується | Зупинити `AUTO` і вийти |
| `ERROR` | Не використовується | Не використовується | Не використовується |

## 6. Пункти меню

| Пункт меню | Дія |
|---|---|
| `1.Auto force` | Автоматичний режим по зусиллю |
| `2.Presets` | Вибір готового пресету |
| `3.Calibration` | Калібрування датчика сили |
| `4.Settings` | Налаштування цільового кута |
| `5.Manual + zero` | Повернення в ручний режим, скидання активного пресету, home/zero |

## 7. Робота з Presets

### Вибір пресету

1. Відкрийте `Menu`.
2. Оберіть `2.Presets`.
3. Крутіть енкодер для вибору потрібного пресету.
4. Натисніть кнопку енкодера.
5. Після цього:
   - пресет стане активним
   - цільове зусилля встановиться автоматично
   - відбудеться повернення на головний екран

### Вихід без вибору пресету

1. На екрані `PRESET` натисніть `JOY_DOWN`.

### Повернення в ручний режим без пресету

1. Відкрийте `Menu`.
2. Виберіть `5.Manual + zero`.
3. Після цього:
   - активний пресет скидається
   - стапель повертається на головний екран
   - виконується home/zero

## 8. Робота з Settings

Екран `Settings` використовується для встановлення цільового кута.

### Зміна цільового кута

1. Відкрийте `Menu`.
2. Виберіть `4.Settings`.
3. Крутіть енкодер для зміни `Angle target`.
4. Щоб зберегти:
   - натисніть кнопку енкодера
5. Щоб вийти без збереження:
   - натисніть `JOY_DOWN`

## 9. Робота з кутом

### Обнулення кута

1. Встановіть механізм у потрібну початкову кутову позицію.
2. Натисніть кнопку `ZERO`.
3. Поточний кут буде прийнятий за новий нуль.

### Контроль кута

- на головному екрані показується поточний кут і цільовий кут
- коли цільовий кут досягнутий, з'являється `DONE!`

## 10. Робота з AUTO

### Запуск AUTO

1. Відкрийте `Menu`.
2. Виберіть `1.Auto force`.
3. На екрані `AUTO` натисніть `JOY_UP`.
4. Стапель почне автоматичний цикл досягнення заданого зусилля.

### Зупинка AUTO

1. На екрані `AUTO` натисніть `JOY_DOWN`.
2. Цикл буде зупинений.
3. Відбудеться повернення на головний екран.

## 11. Калібрування датчика сили

### Порядок калібрування

1. Відкрийте `Menu`.
2. Виберіть `3.Calibration`.
3. Виконуйте підказки на екрані:
   - приберіть навантаження
   - підтвердьте
   - встановіть еталонний вантаж
   - підтвердьте
   - задайте масу еталона енкодером
   - підтвердьте
4. Після завершення параметри будуть збережені.

## 12. Аварійна зупинка

### Використання кнопки STOP

1. Натисніть `STOP` у будь-який момент небезпечної роботи.
2. Мотор аварійно зупиниться.
3. На екрані з'явиться повідомлення помилки.
4. Після усунення причини натисніть `STOP` ще раз.
5. Відбудеться повернення на головний екран.

## 13. Коротка пам'ятка для оператора

| Дія | Що натискати |
|---|---|
| Рух вгору | `JOY_UP` |
| Рух вниз | `JOY_DOWN` |
| Вхід у меню | Натиснути і відпустити кнопку енкодера |
| Вибір пункту меню | Кнопка енкодера |
| Назад з меню | `JOY_DOWN` |
| Тонка підстройка | Утримувати кнопку енкодера і крутити енкодер |
| Зміна сили | Крутити енкодер на головному екрані |
| Обнулити кут | `ZERO` |
| Аварійна зупинка | `STOP` |
| Ручний режим без пресету | `Menu -> Manual + zero` |

## 14. Важливі зауваження

- Перед початком роботи перевіряйте цільове зусилля.
- Після роботи з пресетами, якщо потрібен чистий ручний режим, використовуйте `Manual + zero`.
- Після обнулення кута переконайтесь, що механіка знаходиться у правильній базовій позиції.
- Якщо виникла помилка, спочатку усуньте причину, а вже потім скидайте її кнопкою `STOP`.
- Не залишайте стапель без нагляду під час руху в автоматичному режимі.

## 15. Позначка для друку

Рекомендація для друку:

- формат аркуша: `A4`
- орієнтація: `портрет`
- масштаб: `100%`
- друкувати двосторонньо, якщо потрібно
